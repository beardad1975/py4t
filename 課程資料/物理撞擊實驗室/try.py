from 物理模組 import *
舞台 = 物理引擎(800,800)

def 柱(x, y) :
    pass

def 按下鍵盤時(按鍵, x, y):
    if 按鍵 == key.LEFT :
        柱(x, y)
    


def 按下滑鼠時(x, y):
    物體 = 新增圓球(半徑=20)
    物體.位置 = [0, y]
    向量 = [2000, 200]
    物體.施加衝力(向量)
    


模擬主迴圈()
